# Copy this file to .env in your OpenClaw config directory
# (e.g. the parent folder of this workspace, or ~/.openclaw)
# Then paste your real keys. Never commit .env to git.

# Kimi (Moonshot) — get a key at https://platform.moonshot.ai/
MOONSHOT_API_KEY=

# Optional: other providers (if you add them in openclaw.json)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
