# Scheduling `run-autonomous`

Use the repo template and installer (resolves paths; handles Desktop → `~/Openclaw`):

```bash
cd /path/to/Openclaw
bash tools/install_gerald_autonomous_launchd.sh
```

Template: `launchd/com.gerald.run-autonomous.plist` (Mon/Wed/Fri 09:15).
